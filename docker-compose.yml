services:
  cybersentinel:
    build: .
    image: cybersentinel-dashboard:latest
    container_name: cybersentinel-dashboard
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data:ro
    restart: unless-stopped
